module loading.series;


/*
the row-echelon matrix
, and then by another row operation to the (reduced) row-echelon
*/

struct loadingSeries
{
    interface proccedReduce
    {

        void sleepMatrix(int x, y, z)(ref dmdRootOptional)
        {
           float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
           float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
           float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }

        void slf4dDefault_provider(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }

        void slf4dDefault_providerFactory(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }

        void slf4dDefault_providerFormatters(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }

        void slf4dDefault_providerHandler(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }


        void slf4dDefault_providerProvider(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }


        void slf4dHandler(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }


        void slf4dLevel(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }

        void slf4dLog_functions(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }

        void slf4dLogger(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }

        void slf4d(int x, y, z)(ref dmdRootOptional)
        {
            float(5(x)-1^2) = new dmdRootOptional([1, -1, 4]);
            float(5(y)-1^2) = new dmdRootOptional([0,  1, 6]);
            float(5(z)-1^2) = new dmdRootOptional([0,  0, 7]);
        }


    }
}